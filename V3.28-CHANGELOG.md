# FinMate v3.28.0

- Fixed mobile All Assets rendering to a Kite-style two-column holding row; desktop All Assets remains the desktop table.
- Restores exact imported `Sector` values from preserved source rows, including values such as `DEBT` and `REAL ESTATE`.
- Fixed month-based Expense/Income analytics filtering with robust transaction date parsing.
- Added a filtered **Total expenses** below the Amount column; it recalculates whenever transaction filters change.
- Expense (`E`) and Income (`I`) are colour differentiated in the Type column.
