# FinMate v3.19.0

## Bug fix
- Fixed the Wealth view crash: an accidentally inserted Transactions filter template referenced undefined transaction variables (`all` and `filters`) while rendering the Assets table.
- Restored the Wealth asset table Type heading to a plain heading; each asset's editable Type dropdown remains in its row.
- Bumped the service-worker cache identifier so deployed clients can fetch the fixed application assets.

Validation: JavaScript syntax check passed. No browser end-to-end testing performed.
